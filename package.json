{
  "name": "trading-bot",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute trading-bot --file=schema.sql --remote"
  }
}
